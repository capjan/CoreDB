namespace Core.DB.SQLite.Extensions
{
    public static class SQLiteServerRelated
    {
        //public static IDBDatabase CreateDatabase(this SQLiteServer server, string path, bool allowOverride = false)
        //{
        //    var settings = new SQLiteDatabaseSettings(path);
        //    return server.OpenDatabase(settings, true, allowOverride);
        //}

        //public static IDBDatabase CreateDatabase(this SQLiteServer server, bool allowOverride = false)
        //{
        //    var settings = new SQLiteDatabaseSettings(server.Path);
        //    return server.OpenDatabase(settings, true, allowOverride);
        //}

        //public static IDBDatabase OpenDatabase(this IDBDatabase server, string path)
        //{
        //    var settings = new SQLiteDatabaseSettings(path);
        //    return server.OpenDatabase(settings);
        //}

        //public static IDBDatabase OpenDatabase(this SQLiteServer server)
        //{
        //    var settings = new SQLiteDatabaseSettings(server.Path);
        //    return server.OpenDatabase(settings, false, false);
        //}
    }
}
